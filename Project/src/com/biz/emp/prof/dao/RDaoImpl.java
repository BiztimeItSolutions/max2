package com.biz.emp.prof.dao;


/*import java.util.List;
import org.hibernate.SessionFactory;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Repository;

import com.biz.emp.prof.pojo.RetreivingPojo;


@Repository("RetreivingDao")
public class RDaoImpl implements RetreivingDao {

	@Autowired
	private SessionFactory sessionfactory;

	@Override
	public void saveUser(RetreivingPojo user) {
		sessionfactory.getCurrentSession().saveOrUpdate(user);
	}

	@Override
	public List<User> getUser() {
		@SuppressWarnings("unchecked")
		List<User> userlist = sessionfactory.getCurrentSession()
				.createCriteria(User.class).list();
		return userlist;
	}

	
	
}*/
